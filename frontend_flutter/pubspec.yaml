name: game_hub
description: "Game Hub - 보드게임 모음"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.2.0
  provider: ^6.1.1
  web_socket_channel: ^3.0.1       # WebSocket 통신
  shared_preferences: ^2.3.3       # 토큰 저장

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
